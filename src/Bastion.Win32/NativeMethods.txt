GetWindowRect
SetWindowPos
EnumWindows
SetWinEventHook
UnhookWinEvent
GetAncestor
IsWindowVisible
GetMessage
PeekMessage
TranslateMessage
DispatchMessage
PostThreadMessage
GetCurrentThreadId
WM_QUIT
WM_USER
PEEK_MESSAGE_REMOVE_TYPE
WINEVENT_OUTOFCONTEXT
WINEVENT_SKIPOWNPROCESS
OBJECT_IDENTIFIER
CHILDID_SELF
EVENT_SYSTEM_FOREGROUND
EVENT_SYSTEM_MOVESIZESTART
EVENT_SYSTEM_MOVESIZEEND
EVENT_SYSTEM_MINIMIZESTART
EVENT_SYSTEM_MINIMIZEEND
EVENT_OBJECT_CREATE
EVENT_OBJECT_DESTROY
EVENT_OBJECT_SHOW
EVENT_OBJECT_HIDE
EVENT_OBJECT_LOCATIONCHANGE
EVENT_OBJECT_NAMECHANGE
EVENT_OBJECT_CLOAKED
EVENT_OBJECT_UNCLOAKED

// DESIGN.md §3.2/§4 cloak-burst-to-DesktopSwitchSuspected heuristic (Coalescer, issue #2).
DwmGetWindowAttribute
DWMWINDOWATTRIBUTE

// Window Registry manageability filter and identity resolution (DESIGN.md §3.3, issue #3).
GetWindowLongW
GetClassName
GetShellWindow
GetWindow
GetWindowThreadProcessId
EnumChildWindows
OpenProcess
GetApplicationUserModelId
QueryFullProcessImageNameW
SHGetPropertyStoreForWindow
PROPERTYKEY
PROPVARIANT
PropVariantClear
CoInitializeEx
CoUninitialize
PKEY_AppUserModel_ID
WINDOW_EX_STYLE

// Reconciler heartbeat's per-window authoritative read (DESIGN.md §3.4, issue #4).
// DWMWA_EXTENDED_FRAME_BOUNDS needs no new entry here: it is already generated as a member of the
// DWMWINDOWATTRIBUTE enum requested above for the Coalescer's cloak read (issue #2) -- CsWin32
// generates every member of a requested enum type, not just the ones referenced so far.
IsIconic
IsZoomed

// Placement Executor (DESIGN.md §3.6, issue #5): hang probe, state normalization, invisible-border
// correction, Defer batch apply, verify-after-move.
SendMessageTimeoutW
WM_NULL
SEND_MESSAGE_TIMEOUT_FLAGS
SetWindowPlacement
WINDOWPLACEMENT
WINDOWPLACEMENT_FLAGS
SHOW_WINDOW_CMD
IsWindowArranged
BeginDeferWindowPos
DeferWindowPos
EndDeferWindowPos
SET_WINDOW_POS_FLAGS
SystemParametersInfoW
SYSTEM_PARAMETERS_INFO_ACTION
WIN32_ERROR

// Effective-min-size cache seed (DESIGN.md §6, issue #6): GetSystemMetricsForDpi, never the
// non-DPI-aware GetSystemMetrics, per that API's own documented "should not be used if the
// calling thread is per-monitor DPI aware" guidance -- Bastion's PerMonitorV2 manifest (DESIGN.md
// §8) makes every Bastion thread per-monitor DPI aware. SYSTEM_METRICS_INDEX is listed explicitly
// (mirroring the DWMWINDOWATTRIBUTE precedent above) even though CsWin32 would generate it as
// GetSystemMetricsForDpi's parameter type regardless, so SM_CXMINTRACK/SM_CYMINTRACK are
// discoverable from this file without cross-referencing the generated output.
GetSystemMetricsForDpi
GetDpiForSystem
SYSTEM_METRICS_INDEX

// Tier-1 input service (DESIGN.md §7, issue #7): RegisterHotKey with MOD_NOREPEAT on a dedicated
// pump thread, probed at startup; every registered chord is unregistered from the same thread on
// shutdown (RegisterHotKey/UnregisterHotKey's own documented "by the calling thread" contract,
// identical in spirit to SetWinEventHook/UnhookWinEvent's same-thread requirement above).
// HOT_KEY_MODIFIERS and VIRTUAL_KEY are listed explicitly (same discoverability rationale as
// SYSTEM_METRICS_INDEX/DWMWINDOWATTRIBUTE above) even though CsWin32 would generate both as
// RegisterHotKey's own parameter types regardless.
RegisterHotKey
UnregisterHotKey
HOT_KEY_MODIFIERS
VIRTUAL_KEY
WM_HOTKEY
